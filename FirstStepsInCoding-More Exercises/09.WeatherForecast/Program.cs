// 1. Read from the console a weather string
string weather = Console.ReadLine();
// if weather == sunn print => It's warmm outside else It's cold outside
if (weather == "sunny")
    Console.WriteLine("It's warm outside!");
else
    Console.WriteLine("It's cold outside!");
